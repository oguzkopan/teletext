{
  "name": "functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Modern Teletext",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc && if [ -d lib/functions/src ]; then cp -r lib/functions/src/* lib/ && rm -rf lib/functions; fi && mkdir -p ../lib && cp -r lib/lib/* ../lib/",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "jest --runInBand",
    "test:watch": "jest --watch"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.3.0",
    "firebase-functions": "^5.1.0",
    "axios": "^1.7.0",
    "@google-cloud/vertexai": "^1.4.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.5.0"
  },
  "private": true
}
